# ROQ Configuration

# Production configuration for static generation
quarkus.roq.generator.output-dir=dist
quarkus.roq.generator.batch=false

# Link validation configuration
domtrip.links.validate=true
domtrip.links.fail-on-broken=true
domtrip.links.exclude-external=true

# Link fixing configuration
domtrip.links.fix-internal=true
